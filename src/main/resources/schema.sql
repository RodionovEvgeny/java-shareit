
CREATE TABLE IF NOT EXISTS "User" (
    "User_id" long generated by default as identity PRIMARY KEY,
    "Name" varchar(50) NOT NULL,
    "Email" varchar   NOT NULL
);

CREATE TABLE IF NOT EXISTS "Item" (
    "Item_id" long generated by default as identity PRIMARY KEY,
    "Name" varchar(50) NOT NULL,
    "Description" varchar(300)   NOT NULL,
    "Owner" long   NOT NULL,
    "Available" boolean NOT NULL,
    "Request" long,
    CONSTRAINT "fk_Film_Rating_id" FOREIGN KEY("Owner") REFERENCES "User" ("User_id")
);


--create unique index IF NOT EXISTS "USER_EMAIL_UINDEX" ON "User" ("Email");
--create unique index IF NOT EXISTS "USER_LOGIN_UINDEX" ON "User" ("Login");


